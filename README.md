# project-1752422506073-957

A simple todo app with user authentication and the ability to add, edit, and delete tasks.

---

import React from 'react';
export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: 'sans-serif' }}>
      <h1>Welcome!</h1>
      <p>This is a fallback homepage because the AI could not generate a valid app. Please try again with a more detailed prompt.</p>
    </main>
  );
}